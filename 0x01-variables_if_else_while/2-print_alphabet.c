#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 * Description - return a lower case character
 * Return: Always 0 (success)
*/
int main(void)
{
	char ch;
	char ch2;
	 ch2 = tolower(ch);
	 putchar(ch2);
return (0);
}
