#include<stdio.h>


/**
 * main - Entry point
 * Description - print all single digit number greater than 0
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	while (i < 10)
	{
		printf("%d", i);
	}

	return (0);
}
