#include<stdio.h>

/**
 * main - Entry point
 * Description - prints the size of datatypes
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	i = sizeof(int);
	printf("%d", i);
	return (0);
}
